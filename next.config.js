/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['res.cloudinary.com', 'another-domain.com', 'yet-another-domain.com'],
      },
}

module.exports = nextConfig
